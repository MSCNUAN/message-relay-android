# 安全政策

仓库地址：[https://github.com/MSCNUAN/message-relay-android](https://github.com/MSCNUAN/message-relay-android)。

## 请勿公开提交敏感信息

请不要在 Issue、PR、截图、日志或备份中公开：

- Bark Token；
- 飞书或钉钉 Webhook；
- GitHub Token；
- 签名密钥和密码；
- 完整电话号码；
- 验证码；
- 完整通知正文；
- 用户备份和数据库。

## 报告安全问题

如果你发现可能导致隐私泄露、凭证泄露、越权转发或日志输出敏感信息的问题，请不要公开贴出完整敏感值。

可以先通过 GitHub Issue 用脱敏方式说明影响范围，或联系维护者后再提供细节。公开 Issue 中请保留最少必要信息。

## 日志脱敏要求

提交诊断日志前请检查并替换：

- Token 和 Webhook：只保留前后少量字符；
- 电话号码：只保留末尾 2 到 4 位；
- 验证码：全部替换为 `******`；
- 通知正文：只保留能复现问题的最小片段。

## 支持版本

当前仅维护最新公开版本和 `main` 分支。旧测试包不保证持续修复。

## 普通 Bug 与安全漏洞

- 普通 Bug：界面显示错误、构建失败、规则不生效、推送失败等。
- 安全漏洞：敏感信息泄露、未授权发送、日志暴露 Token、备份恢复绕过校验等。
